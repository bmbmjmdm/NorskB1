<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Metro dev server needs network access in debug builds only. -->
    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
